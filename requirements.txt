# Modern MarioRL stack — tested 2026-09 with Python 3.13.5 / 3.14.7 on macOS arm64
# See README.md for setup. Notebook's Environment Check cell also validates versions.
# Loose pins (>=) for flexibility; remove >= to pin exact tested versions if you need reproducibility.

# Core RL env (Gymnasium-native, fixes numpy 2.x uint8 overflow)
gymnasium>=1.3.0
gym-super-mario-bros>=9.1.0
nes-py>=9.0.1

# RL & DL
stable-baselines3[extra]>=2.9.0
torch>=2.4.0
# torchvision/torchaudio not needed for this tutorial (CnnPolicy uses torch only)

# Utilities
numpy>=2.0
matplotlib>=3.8
tqdm>=4.60
rich>=13.0
tensorboard>=2.12
cloudpickle>=3.0

# Jupyter
jupyter>=1.0
ipykernel>=6.0
